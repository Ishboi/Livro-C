#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main()
{
    char nome[51;
    int i;
    printf("Diga alguma coisa ");
    scanf("%50[^\n]",nome);
    for(i=0;nome[i];i++)
        if(nome[i]!=' ')// != quer dizer se diferente
        nome[i]
}
